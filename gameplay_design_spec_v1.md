# 马马合体 可玩性优化方案 Spec v1.0（制作人视角）

> 交付对象：Composer 2.5，要求全量、无偏实现。
> 前置依赖：`remediation_spec_v1.md`（下称"整改 Spec"）。本 Spec 假定整改 Spec 的 P0（bug 修复）、T4-1（音效）、T4-3（balance.json）已完成或与本 Spec 同批实现；引用处会显式标注。
> 硬约束：**零新增美术资产**。全部改动只用代码、现有贴图、程序化绘制（Graphics/Text/粒子/tween）实现。

---

## 第一部分：制作人评估——这个游戏现在好玩吗

### 1.1 值得保留的资产（不要动）

- **题材与幽默感是最大卖点**：105 个谐音梗/文化梗角色（竹马→马冬梅→马一龙→宇宙神驹）、角色祝语、Boss 台词，收集驱动力真实存在。
- **骨架完整**：合成链 + Boss 战 + 道具商店 + 四路线真结局，三层机制齐全，超过同类节点档期小游戏的平均完成度。
- **终章分流设计（封卷 vs 大和谐）** 给了玩家「见好就收 or 赌一把」的真选择，是全作最好的一个设计决策。

### 1.2 核心问题诊断（按严重度排序）

**问题一：成长即通胀，玩家没有「经营感」（最伤玩法根基）**

`Grid.ts` 生成逻辑：80% 的新棋子直接落在 `[棋盘最高级−4, 最高级−2]`，20% 落在最低活跃级。这意味着玩家几乎永远有「现成的高级对子」可合，升级是自动发生的通胀，而不是玩家规划出来的成果。2048 的核心乐趣——保护角落大数字、规划合成链条、为一次大合并铺垫十几步——在这里被系统送分稀释掉了。玩家的最优策略退化成「无脑滑」。

**问题二：张力曲线是平的，死不了也就爽不了**

三重保险叠加：空位 < 3 时 60% 概率直接送大炮、空盘 < 4 子时保底送低级、动态地板自动清场。结果是常规死局几乎不可能发生，失败威胁不存在；仅有的压力来自 Boss 障碍堆积——那是挫败感（我没法操作）而不是策略张力（我犯了错）。没有濒死就没有翻盘的爽感。

**问题三：一局超长且不能中断（留存杀手）**

到 Lv.101 需要重复同一循环上百次，估计单局数小时；而 `Game` scene 每次进入全部重置，**没有局内存档**。手机玩家一旦切后台/锁屏刷新就前功尽弃。这是当前最致命的产品问题，比任何机制问题优先。

**问题四：101 级 = 同一个循环重复 100 次**

除 Boss 换皮、角色换图外，Lv.10 和 Lv.90 的玩法完全一致。没有章节目标、没有里程碑奖励、没有节奏变化，中段（Lv.30–90）会非常无聊。终章（91+）反而是设计最丰富的区段——但绝大多数玩家撑不到那里。

**问题五：决策密度低，反馈密度也低**

- 无「下一个生成」预览 → 滑动方向的选择缺乏信息支撑，谈不上策略。
- 技能一次性点按即放，无目标预览、无权衡（不用白不用）。
- 金币跨局滚存无上限 → 玩几局后道具随便买，经济系统失效。
- 无连击/连锁计分、无浮动分数、无暴击仪式感、无音效（整改 Spec T4-1 已列）——合成的「juice」几乎为零。

**问题六：真结局路线不可感知**

四风格路线由后台统计打分决定，玩家全程看不到自己偏向哪条路线，无法主动追求想要的结局 → 结局分流的重玩价值没有兑现。

### 1.3 结论

现在的状态是「有完整骨架、有优秀题材，但核心循环缺乏张力与掌控感，且一局玩不完」。以下方案全部为程序可解，按对可玩性的边际收益排序为三波：Wave A（生死线）→ Wave B（循环深度）→ Wave C（重玩价值）。

---

## 第二部分：实施方案

> 通用约束：
> 1. 所有新数值一律进 `frontend/src/data/balance.json`（整改 Spec T4-3 的产物），代码不留魔法数字。
> 2. 所有新 UI 用现有资源 + Graphics/Text 绘制，遵守 requirements.md 3.4 的布局条款（不遮首排、层级、安全区），复用整改 Spec T3-2 的 `layout.ts`。
> 3. 音效挂点统一走整改 Spec T4-1 的 `AudioManager`。
> 4. 每个任务完成后同步勾选 `project planning.md` Phase 8 对应项。

---

## Wave A：生死线（必做，优先于一切 Wave B/C）

### G1 局内存档与断点续玩

- 新增 `frontend/src/managers/RunStateManager.ts`：
  1. 序列化内容：棋盘每格 `{charId | itemId | obstacleType, frozen, skillUsed}`、分数、本局金币收支、背包、Boss 状态（id/hp/回合计数/障碍位）、endgame phase、大和谐共鸣计数与事件数、路线统计累计值、`minActiveLevel/maxProgressLevel`、随机种子（若 G9 实现）。
  2. 时机：每次有效滑动结算完成后写入 `localStorage`（key `horse_merge_run_v1`），JSON 体积预计 < 10KB，无性能问题；写入用 try/catch 包裹。
  3. 结算（进入 GameOver）或玩家主动放弃时清除存档。
- `MainMenu` 改造:存在有效存档时主按钮显示「继续上局（Lv.XX · 分数 XXXX）」，副按钮「重新开始」（点击弹确认并清档）。
- `Game.create()` 支持从快照恢复：所有 tile 按存档重建（贴图未加载时走整改 Spec T1-2 的灰块兜底），Boss/HUD/阶段面板同步恢复。
- 版本安全：快照带 `schemaVersion` 字段，不匹配时静默弃档。
- AC：游戏中途刷新页面 → 主菜单出现继续入口 → 恢复后棋盘、分数、金币、Boss 血量、背包、终章阶段与刷新前一致；结算后继续入口消失；手动写坏 localStorage 值不导致崩溃（弃档新开）。

### G2 生成节奏重构——把「经营感」还给玩家

- 目标：让升级重新成为玩家规划的结果。全部参数入 `balance.json` 的 `spawn` 节：
  1. 常规期生成分布改为三段：**50%** 低段 `[minActive, minActive+1]`（原 20%）、**35%** 中段 `[max−5, max−3]`、**15%** 高段 `[max−3, max−2]`（原 80% 集中高段）。
  2. **濒死送大炮**：概率 60% → **25%**，且增加冷却——每 10 次生成最多触发 1 次（计数器随生成递增）。
  3. **空盘保底**（<4 子送低级）保留不变（这是合理的防呆）。
  4. 动态地板（低级清场）阈值提升速度减半：现有实现是随 maxLevel 提升 `minActiveLevel`，改为每 2 个 maxLevel 提升 1（参数化为 `floorRiseRatio: 0.5`），让玩家多背几级低级库存、增加盘面管理压力。
  5. 终章（ascension/harmony）分布维持现状（该区段整改后的节奏是对的）。
- 兼容性：requirements.md 3.1「节奏保护」「清场保护」条款继续满足（本改动只收紧、不放宽跃迁）。
- AC：新开一局玩 50 步，统计生成日志（DEV 模式 console.table 输出生成 level 分布），三段比例与配置一致（±5%）；濒死连续触发大炮的间隔 ≥ 10 次生成；主观验证：Lv.20 之前玩家需要主动囤低级对子才能持续升级。

### G3 「下一个」预览 + 悔棋

- **Next 预览**：生成逻辑拆为「预抽」与「落子」两步——每次滑动结算时先抽好下一个棋子（等级+角色），显示在顶栏分数旁（48×48 小框：角色贴图缩放 + 等级角标，Graphics 画框）；下次滑动时落的就是它。特殊生成（大炮救济、Boss 弱点、配方助攻）同样走预抽队列，玩家可见。
- **悔棋（回天）**：每局 1 次免费。滑动前保存整盘快照（复用 G1 的序列化），悔棋按钮放在菜单按钮旁（Text 按钮「悔」+次数角标）；使用后回滚棋盘/分数/生成队列，Boss 回合数不回滚（防止刷 Boss）；用尽后按钮置灰。免费次数与「是否允许金币购买额外次数（默认 false）」进 balance.json。
- AC：预览显示的棋子与实际落子一致（连续验证 20 步）；悔棋后盘面与滑动前完全一致且预览队列同步回滚；每局只能用 1 次；悔棋不影响 Boss 已推进的回合。

### G4 操作手感包（juice，零美术）

全部开关与参数入 `balance.json` 的 `feel` 节：

1. **输入缓冲**：动画播放期间收到的下一次滑动入队（队列深度 1），动画结束立即执行——消除现在快速滑动被 debounce 吃掉的粘滞感。
2. **合成挤压弹跳**：合并落点 tile 做 squash & stretch（scale 1.25→0.9→1.0，Back.easeOut，180ms），替换现有单调缩放。
3. **浮动分数**：每次合成在落点上浮 `+XXX` Text（tween 上移 60px + 渐隐 600ms），暴击时字号 ×1.5、金色、附加 `Cameras.main.shake(80, 0.004)`。
4. **连击（Combo）计分**：一次滑动触发 ≥2 次合成（含自动连锁）时，第 N 次合成得分 ×(1 + 0.25×(N−1))，上限 ×3；棋盘上方显示「连击 ×N」Text（复用瞬时提示层级规范），combo 音效音调随 N 递增（AudioManager 合成时 pitch ×1.06^N）。
5. **里程碑慢动作**：首次合成出每个 Tier 首个角色（Lv.21/41/61/81/91/101）时，`this.time.timeScale` 短暂 0.3（400ms 真实时间）+ 镜头轻微 zoom-in 1.06 再回弹——用引擎时间缩放实现，零资产。
6. **移动端震动**：合成 `navigator.vibrate?.(15)`、暴击 40ms、Boss 击败 [30,50,80] 模式；桌面端自动无效，无需分支报错。
7. **音效挂点**：以上全部接 AudioManager（merge/critical/combo/milestone），依赖整改 Spec T4-1。
- AC：快速连续滑动 10 次无丢输入、无卡死（回归 planning「快速滑动卡死修复」条款）；连击得分按公式验证 3 组；慢动作只在 Tier 首达时触发一次；所有效果可通过 balance.json 单独关闭。

### G5 张力与自救的再平衡

- **盘面压力条**：棋盘上边缘用 Graphics 画 4px 细条，颜色随占格率渐变（<70% 不显示，70–85% 黄，>85% 红 + 呼吸闪烁），配合 requirements 布局条款（不占新空间、不遮首排）。
- **濒死警告**：占格率首次 >85% 时瞬时提示「盘面告急！」（每局最多提示 2 次，防骚扰）。
- **暴击保底**：暴击率维持 5%（balance.json 已有），新增 pity：连续 40 次合成未暴击则下次必暴击，计数器入 G1 存档。
- **死局结算缓冲**：判死前给一次「最后审视」——高亮所有仍可行动的格子 1.5 秒（若有则不判死，这是整改 Spec T0 已修的逻辑；此处仅加视觉呈现），然后再进结算。让玩家「看着自己死」，死得明白。
- AC：压力条阈值行为正确；pity 计数跨存档持久；死局前有高亮缓冲且不误判（配合整改 Spec 死局校验回归）。

---

## Wave B：循环深度（Wave A 完成后实施）

### G6 章节目标与里程碑奖励

- 新增 `frontend/src/data/chapters.json`：5 个章节（对应 Tier 1–5，沿用现有章节 banner 文案体系），每章 2–3 个目标，全部可由现有统计判定：
  - 例：T1「合成出 Lv.10 / 击败淘气火猴 / 触发 1 次连击×3」；T2「合成 1 次突变配方 / 使用 3 次技能」……（每章目标由实现者按现有机制拟定，规则：只用已有统计事件，禁止新增机制）。
- 目标完成即时奖励金币（数额进 balance.json，建议 100–400 递增），达成时走现有 toast + unlock 音效。
- 常驻章节摘要面板（整改 Spec T3-2 的 GameHud）加一行当前未完成目标（≤12 字），完成后自动切下一条。
- 结算页命运报告追加「本局完成目标 X/Y」。
- AC：每章目标可完成且奖励到账一次（不重复发）；目标文案不超安全区；G1 存档包含目标进度。

### G7 Boss 战节奏改造（从「耗」到「攻防回合」）

全部参数入 balance.json 的 `boss` 节：

1. **技能预告（telegraph）**：Boss 每次放技能前一回合，在 Boss 信息栏显示「下回合：熔岩地核」（数据取 bosses.json 的 skill.name），并让 Boss 贴图播 action 动画一次。玩家获得一回合准备期。
2. **障碍上限**：单 Boss 在场障碍格总数 ≤ 8（可配），达到上限后 Boss 技能改为对玩家金币/分数的小额惩罚（沿用 miasma 偷金逻辑），杜绝「棋盘被障碍锁死」的挫败。
3. **激怒计时**：Boss 在场超过 N 回合（默认 25）进入激怒：技能频率从每 3 回合改为每 2 回合，Boss 信息栏显示红色「激怒」。给拖延施加代价，制造「速杀 or 稳杀」的决策。
4. **击败三选一**：Boss 击败后弹出三选一卡（复用 Lv.101 分流卡的排版规范与 `ending_choice_card` 底图）：「金币 ×N ／ 随机道具 1 个 ／ 下一个生成必为当前最高级−1」。选择即生效。
- AC：预告与实际技能一致；障碍数封顶；激怒回合与频率符合配置；三选一每个选项验证生效；全部状态入 G1 存档。

### G8 经济重构（终结滚存通胀）

- 现状：金币跨局滚存无上限 + 初始 1000，三局之后道具自由，商店失去意义。
- 方案（保守改造，不推翻既有结构）：
  1. 金币拆两账：**局内金币**（本局赚本局花，结算时按 10:1 折算成分数加进总分）与**传承金币**（跨局持久，上限 2000，仅用于开局补给——新局开始时可花传承金币在「开局补给页」买 1–2 件道具带入，价格为商店价 8 折）。
  2. 局内所有获取（合成、arch_economy、Boss 掉落）进局内账；结算时超出部分折分。
  3. 传承金币来源：结算时按结局质量注入（死局 100 / 封卷 300 / 真结局 800，进 balance.json）。
  4. 商店价格随章节递增：`price × (1 + 0.3 × (tier − 1))`，抑制后期道具白菜价。
  5. SaveManager 迁移：读到旧版 `horse_merge_gold` 时一次性转换（min(旧值, 2000) 入传承账），不让老玩家清零。
- 开局补给页：MainMenu「开始游戏」后、Game 创建前的一个轻量弹层（复用卡面排版），可跳过。
- AC：两账分离且结算折算正确；传承上限生效；旧存档平滑迁移；商店价格随章节变化；跳过补给页可直接开局。

### G9 技能目标预览与确认

- 现状：技能点击立即释放，无预览无确认，误触成本高、决策感为零。
- 改造：点击技能按钮后进入「瞄准态」——按 `skill.type` 高亮将受影响的格子（bomb 显示 3×3 范围框、clear_low 高亮所有低级格、snipe 高亮 Boss、transform/upgrade 高亮目标格……用 Graphics 半透明色块，风格区分增益绿/伤害红）；再次点击确认释放，点击空白处取消。
- 需要选目标的技能（如 upgrade 邻格）在瞄准态支持点选目标格。
- 全程 300ms 内可完成（不拖慢老玩家）：瞄准态下直接再点按钮=以默认目标释放。
- AC：每类 arch_* 技能的高亮范围与实际效果格完全一致（逐类验证）；取消不消耗技能；瞄准态不阻塞滑动输入超过必要范围（进入瞄准态时滑动无效，取消后恢复）。

---

## Wave C：重玩价值（Wave B 完成后实施）

### G10 路线倾向仪表（让真结局分流可感知、可追求）

- 在大和谐进度卡（现有底部状态卡）常驻显示四路线倾向条：取 `Game.ts` 现有路线打分的中间值，归一化为 4 条 mini bar（Graphics，赛博紫/古风金/动漫粉/神话青，色值定义进 balance.json），领先路线名加亮。
- 每次合成 SSR/Hidden 角色后若领先路线变化，toast 提示「命运偏向了【古风经典】」。
- 图鉴中已解锁的真结局卡详情页追加一行「达成提示：多合成神话系角色可导向此结局」（四条文案硬编码在 characters.json 各隐藏卡的新字段 `routeHint`）。
- AC：倾向条与最终判定路线一致（打到真结局验证 2 条不同路线）；未进入终章时仪表隐藏（避免信息过载）。

### G11 每日种子挑战

- MainMenu 新增「今日挑战」入口：以 `YYYYMMDD` 为随机种子（Phaser `Math.RND.init([seed])` 或自实现 mulberry32，**全局替换 Grid/BossManager 中的 Math.random 为注入的 rng 函数**——这是本任务主要工作量，需在 G2 之前评估：若整改 Spec T3-1 已把生成逻辑收进 SpawnSystem，则只需在 3–4 个模块注入）。
- 规则：每日同种子 + 固定初始盘面 + 关闭悔棋与开局补给；结算页显示「今日挑战 · 种子 XXXX」与专属分享文案。
- 每日最佳分数存 localStorage（`horse_merge_daily_{date}`），主菜单显示今日是否已挑战及最佳分。
- AC：同一天两次进入今日挑战，前 20 步生成序列完全一致；普通模式不受种子影响；跨天种子变化。

### G12 配方发现系统

- 图鉴新增「配方」页签：每条 recipe 一行，未发现时显示「？？？ + ？？？ → ？？？」（已知一半材料时显示已知侧），首次触发某配方时标记发现、给一次性金币奖励（200，入 balance.json）并 toast「发现新配方：马后炮！」。
- 发现状态存 SaveManager（新 key `horse_merge_recipes_found`）。
- 依赖整改 Spec T4-2（配方扩到 15 条）后实施，否则内容太薄。
- AC：15 条配方的发现/未发现状态正确持久；奖励只发一次；页签排版符合图鉴现有分页规范。

---

## 实施顺序与依赖总图

```
整改 Spec P0（bug）+ T4-3（balance.json）  ←—— 一切的前置
        │
Wave A：G2 生成重构 → G3 预览+悔棋 → G4 手感包（依赖 T4-1 音效）→ G1 局内存档 → G5 张力平衡
        │   （G1 放在 G3 之后做，因为存档 schema 要包含预抽队列与悔棋次数）
Wave B：G6 章节目标 → G7 Boss 改造 → G8 经济重构 → G9 技能预览
        │   （若整改 Spec T3-1/T3-2 重构已完成，Wave B 直接在新模块结构上写；未完成则先做重构）
Wave C：G10 路线仪表 → G12 配方发现（依赖 T4-2）→ G11 每日种子（工作量最大，放最后）
```

## 总验收（全部完成后）

1. 一局中途刷新 3 次均可无损续玩（Wave A 核心验证）。
2. 新手体验轴测试：开新档玩到 Lv.30，确认（a）需要主动囤低级对子，（b）至少触发 1 次连击与 1 次里程碑慢动作，（c）完成至少 3 个章节目标，（d）经历 1 次盘面告急并自救成功或死局。
3. Boss 轴：击败 2 个 Boss，验证预告→激怒→三选一全链路。
4. 经济轴：连玩 2 局，传承金币按结局注入且不超上限，第二局开局补给可用。
5. 终局轴：打出 1 次真结局，路线仪表全程与最终判定一致。
6. 每日挑战同种子复现验证。
7. 整改 Spec 附录 A 的全量回归清单重跑一遍（本 Spec 改动不得破坏整改成果）。

## 明确不做的事（防止实现跑偏）

- 不新增任何角色、贴图、音频文件（音效走 T4-1 程序化合成）。
- 不改棋盘尺寸（6×6 不动）、不改 101 级角色链、不改四路线真结局判定公式（G10 只是可视化）。
- 不加体力/广告/内购等商业化机制。
- 不做多人/排行榜后端（与整改 Spec T4-4 纯前端决策一致）。
