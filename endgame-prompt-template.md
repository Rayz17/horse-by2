# 真结局 Prompt 使用说明

- 实际生图时，不要使用本文件自行拼装或改写。
- 直接打开 `endgame-variant-prompts.md`，复制对应风格的完整 `Base Prompt` 整段使用。
- `endgame-cast-manifest.md` 仅作为角色真源留档，不需要随 prompt 一起上传。
- `endgame-ceremony-art-bundle.md` 仅用于查看资源文件名、尺寸和交付清单。

## 直接使用规则
- 生立绘：复制 `endgame-variant-prompts.md` 中对应风格的 `Base Prompt` 全文。
- 真结局在结算页直接结束，不再需要额外动作条资源。
- 不要再额外“增强”“补充说明”“重新组织句子”，避免角色一致性被改坏。
