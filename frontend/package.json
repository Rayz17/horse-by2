{
  "name": "horse-merge-2026",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync-data": "node scripts/sync-data.mjs",
    "validate-data": "node scripts/validate-data.mjs",
    "fix-skill-desc": "node scripts/fix-skill-desc.mjs",
    "reallocate-skills": "node scripts/reallocate-skills.mjs",
    "predev": "npm run sync-data",
    "prebuild": "npm run sync-data && npm run validate-data",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "playwright": "^1.49.1",
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  }
}
